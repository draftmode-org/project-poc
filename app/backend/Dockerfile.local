ARG IMAGE_NAME
ARG IMAGE_TAG
ARG NGINX_IMAGE_NAME=nginx
ARG NGINX_IMAGE_TAG=1.23.3-alpine

FROM ${IMAGE_NAME}:${IMAGE_TAG} as local
WORKDIR /var/app

FROM ${NGINX_IMAGE_NAME}:${NGINX_IMAGE_TAG} as webserver
WORKDIR /var/app
